"""Grouped argument definitions assembled by ``allthemix.cli.args``.

One module per domain; each exposes ``add_<domain>_arguments(parser)``.
Flag behavior is defined here verbatim; ``args.py`` only assembles and
post-processes.
"""
