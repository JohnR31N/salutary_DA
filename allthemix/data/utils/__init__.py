"""Data-domain utilities: small helpers shared across the data pipelines.

- ``cardinality``        exact train-set example-count resolution
- ``normalization``      dataset normalization statistics
- ``random``             deterministic data-side randomness helpers
- ``source_replacement`` source-aligned replacement for offline artifacts
"""
